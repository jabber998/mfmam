.pyc/
__pycache__/
*.pyc

# hasil build (dibangkitkan build.py dari site-content/)
dist/
# data R2 (dibangkitkan build.py; di-upload ke bucket R2 mfmam-data)
dist-data/
manga/
genre/
daftar-komik/
page/
kontak/
raw/
chapters-index.json
search.json
sitemap.xml
404.html
_redirects

# data scraping TIDAK di-commit ke git (di-deploy via Cloudflare Pages &
# di-restore dari situs live oleh _restore_data.py). File ditulis scraper.
site-content/series/
site-content/scrape-state.json
site-content/manual-batch.txt

# kredensial & cache mirror gambar Blogger (TIDAK di-commit)
site-content/blogger-creds.json
site-content/blogger-meta.json
site-content/blogger-image-map.json
.blogger-client.json

# artefak uji lokal
test_img.bin
err.txt

# artefak build LAMA (model Netlify: halaman bab per-judul di root).
# Build baru menulis ke dist/ (di atas); folder lama dibiarkan di disk
# tapi jangan ikut ter-commit.
chapter-*/
[a-z0-9]*-chapter-*/
*-official-english/
*-raw/

# artefak hosting lama (sudah migrasi ke Cloudflare Pages)
netlify.toml
vercel.json
_netlify_build.py

# kredensial login wrangler (OAuth) & log lokal
.wrangler/
*.log
*.log.err

# README & helper yang MURNIO LOKAL (tools/operasi, kredensial, VPN) —
# tidak boleh ikut commit/push. Dokumentasi ini describes materi lokal.
README-LOKAL.md
